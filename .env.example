# Railway PostgreSQL Database URL
# For local development, use your Railway database URL
# Railway will automatically set this in production
DATABASE_URL=postgresql://postgres:password@host:port/railway

# --- Supabase migration (optional) ---
# Use these only when running scripts/migrate_postgres_to_supabase.py
# SOURCE_DATABASE_URL=postgresql://postgres:password@host:port/railway
# DEST_DATABASE_URL=postgresql://postgres:<SUPABASE_PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres?sslmode=require
